[
    {client, [{test, true}]},
    {server, [{port, 6969}]},
    {kernel, [
        {logger_level, info},
        {logger, [
            {handler, default, logger_std_h, #{
                level => info,
                formatter => {logger_formatter_json, #{}}
            }}
        ]}
    ]}
].
